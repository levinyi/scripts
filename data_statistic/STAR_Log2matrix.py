import sys
import re


class STARLog:
    def __init__(self):
        total_reads = self.total_reads
        unique_mapped_reads = self.unique_mapped_reads
        number


def deal_with_star_log():
    alist = []
    with open() as f:
        for line in f:
            line = line.s
    return alist

for each_file in sys.argv[1:]:
    name = each_file.split(".")[0]
    deal_with_star_log()
